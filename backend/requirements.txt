# SkillTwin AI —— 后端依赖
#
# 安装方式（复用已有 torch，避免重复下载 2.4GB）：
#     python -m venv .venv --system-site-packages
#     .venv/Scripts/python.exe -m pip install -r backend/requirements.txt
#
# 注意：Web 栈必须钉版本。全局环境若存在 starlette>=0.42 会与 fastapi 0.115 冲突
# （Router.__init__() got an unexpected keyword argument 'on_startup'），
# 装进 venv 可正确遮蔽全局版本。

# ---------- Web 框架（版本强绑定，勿随意放宽） ----------
fastapi==0.115.6
starlette==0.41.3
uvicorn[standard]==0.32.1
python-multipart>=0.0.9

# ---------- 配置与校验 ----------
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
python-dotenv>=1.0.1

# ---------- 数据库 ----------
SQLAlchemy>=2.0.30,<2.1.0
alembic>=1.14.0,<2.0.0

# ---------- LLM 客户端（OpenAI 兼容：DeepSeek / Qwen / OpenRouter） ----------
openai>=1.50.0,<2.0.0
httpx>=0.27.0

# ---------- RAG ----------
chromadb>=1.0.0,<2.0.0
sentence-transformers>=3.0.0      # 本地 embedding，依赖 torch
rank-bm25>=0.2.2                  # 稀疏检索，补 dense 对术语的漏召
jieba>=0.42.1                     # 中文分词（BM25 用）

# torch 不在此列出：CUDA 版本需从官方索引安装，且本机已具备
#   torch 2.2.2+cu121（RTX 3050）
# 全新环境如需 GPU 支持：
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# 纯 CPU 环境直接 `pip install torch` 即可，embedding 会自动回落到 CPU。

# ---------- 文档解析 ----------
pypdf>=5.0.0
python-docx>=1.1.0

# ---------- 测试 ----------
pytest>=8.0.0
pytest-asyncio>=0.24.0
