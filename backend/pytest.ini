[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers --tb=short
markers =
    llm: 需要真实 LLM API Key 才能运行（默认跳过）
    slow: 需要加载本地模型等耗时操作
filterwarnings =
    ignore::DeprecationWarning:pydantic.*
    ignore::DeprecationWarning:chromadb.*
